const width = 600;
const height = 600;

function setup() {
  const canvas = createCanvas(width, height);
  canvas.parent("p5canvas");
  background(64, 128, 128);
  colorCounter = 0;
  colorMode(HSB, 1);
  strokeWeight(5);
}

function draw() {

}